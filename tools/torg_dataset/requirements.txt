# TØR-G Dataset Generation Tools

# Core dependencies (required)
# (none - uses stdlib only for generation)

# Optional: HuggingFace upload
datasets>=2.14.0
huggingface_hub>=0.19.0

# Optional: LLM-augmented generation (Stage 3)
anthropic>=0.34.0
